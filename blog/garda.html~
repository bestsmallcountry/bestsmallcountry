---
layout: default
title: Blog
description: A personal account of dealings with Irish officialdom.
---

<h2>Garda</h2>



<div class="home">

<ul class="post-list">
	{% for post in site.posts %}
	{% if post.category contains "Garda " %}
    <li>
	<span>
		<div>
			<span class="post-meta">{{ post.date | date: "%b %-d, %Y" }}
			{{ post.category }}: {{ post.source }}</span>
		</div>
		<span>
    		
			<img src="/images/svg/garda.svg" alt="Garda Logo" height="50" width="50" >
			<h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
		</span>
	</span>
    </li>
    {% endif %}
  {% endfor %}
</ul>

<p class="rss-subscribe">Feed <a href="{{ "/feed.xml" | prepend: site.baseurl }}">RSS</a></p>
</div>
